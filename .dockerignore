# Build context is usually the repository root (e.g. `apps/web-vite` dev image + CLI).
# API and web-vite images use this context from the repo root (see root `docker-compose.yml.example`).

.git/
.github/
.cursor/

**/target/
**/node_modules/

apps/web/.next/
apps/web/out/
apps/web-vite/dist/

apps/ios/
