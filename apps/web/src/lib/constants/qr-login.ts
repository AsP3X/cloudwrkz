/** Admin-configurable rate limit for QR login requests (max per minute). */
export const DEFAULT_QR_REQUESTS_PER_MINUTE = 20;
export const MIN_QR_REQUESTS_PER_MINUTE = 1;
export const MAX_QR_REQUESTS_PER_MINUTE = 120;
